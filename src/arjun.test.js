const mathOperations = require('./index');

describe("Arjun Calculator tests", () => {
    test('1Arjun adding 1 + 2 should return 3', () => {
        var result = mathOperations.sum(1, 2)
        expect(result).toBe(3);
    });
    
    test('2Arjun difference 3 - 2 should return 1', () => {
        var result = mathOperations.diff(3, 2)
        expect(result).toBe(1);
    });

    test('3Arjun product 3 * 2 should return 6', () => {
        var result = mathOperations.product(3, 2)
        expect(result).toBe(6);
    });
    
    
    test('4Arjun test added by testtas9812 product 3 * 2 should return 6', () => {
        var result = mathOperations.product(3, 2)
        expect(result).toBe(6);
    });
  
        test('Arjun adding 1 + 2 should return 3', () => {
        var result = mathOperations.sum(1, 2)
        expect(result).toBe(3);
    });
    
    test('5Arjun difference 3 - 2 should return 1', () => {
        var result = mathOperations.diff(3, 2)
        expect(result).toBe(1);
    });

    test('6Arjun product 3 * 2 should return 6', () => {
        var result = mathOperations.product(3, 2)
        expect(result).toBe(6);
    });
    
    
    test('7Arjun test added by testtas9812 product 3 * 2 should return 6', () => {
        var result = mathOperations.product(3, 2)
        expect(result).toBe(6);
    });
  
      test('8Arjun adding 1 + 2 should return 3', () => {
        var result = mathOperations.sum(1, 2)
        expect(result).toBe(3);
    });
    
    test('9Arjun difference 3 - 2 should return 1', () => {
        var result = mathOperations.diff(3, 2)
        expect(result).toBe(1);
    });

    test('10Arjun product 3 * 2 should return 6', () => {
        var result = mathOperations.product(3, 2)
        expect(result).toBe(6);
    });
    
    
    test('11Arjun test added by testtas9812 product 3 * 2 should return 6', () => {
        var result = mathOperations.product(3, 2)
        expect(result).toBe(6);
    });
  
      test('12Arjun adding 1 + 2 should return 3', () => {
        var result = mathOperations.sum(1, 2)
        expect(result).toBe(3);
    });
    
    test('13Arjun difference 3 - 2 should return 1', () => {
        var result = mathOperations.diff(3, 2)
        expect(result).toBe(1);
    });

    test('14Arjun product 3 * 2 should return 6', () => {
        var result = mathOperations.product(3, 2)
        expect(result).toBe(6);
    });
    
    
    test('15Arjun test added by testtas9812 product 3 * 2 should return 6', () => {
        var result = mathOperations.product(3, 2)
        expect(result).toBe(6);
    });
  
      test('16Arjun adding 1 + 2 should return 3', () => {
        var result = mathOperations.sum(1, 2)
        expect(result).toBe(3);
    });
    
    test('17Arjun difference 3 - 2 should return 1', () => {
        var result = mathOperations.diff(3, 2)
        expect(result).toBe(1);
    });

    test('18Arjun product 3 * 2 should return 6', () => {
        var result = mathOperations.product(3, 2)
        expect(result).toBe(6);
    });
    
    
    test('19Arjun test added by testtas9812 product 3 * 2 should return 6', () => {
        var result = mathOperations.product(3, 2)
        expect(result).toBe(6);
    });
  
      test('20Arjun adding 1 + 2 should return 3', () => {
        var result = mathOperations.sum(1, 2)
        expect(result).toBe(3);
    });
    
    test('21Arjun difference 3 - 2 should return 1', () => {
        var result = mathOperations.diff(3, 2)
        expect(result).toBe(1);
    });

    test('22Arjun product 3 * 2 should return 6', () => {
        var result = mathOperations.product(3, 2)
        expect(result).toBe(6);
    });
    
    
    test('23Arjun test added by testtas9812 product 3 * 2 should return 6', () => {
        var result = mathOperations.product(3, 2)
        expect(result).toBe(6);
    });
  
      test('24Arjun adding 1 + 2 should return 3', () => {
        var result = mathOperations.sum(1, 2)
        expect(result).toBe(3);
    });
    
    test('25Arjun difference 3 - 2 should return 1', () => {
        var result = mathOperations.diff(3, 2)
        expect(result).toBe(1);
    });

    test('26Arjun product 3 * 2 should return 6', () => {
        var result = mathOperations.product(3, 2)
        expect(result).toBe(6);
    });
    
    
    test('27Arjun test added by testtas9812 product 3 * 2 should return 6', () => {
        var result = mathOperations.product(3, 2)
        expect(result).toBe(6);
    });
  
      test('28Arjun adding 1 + 2 should return 3', () => {
        var result = mathOperations.sum(1, 2)
        expect(result).toBe(3);
    });
    
    test('29Arjun difference 3 - 2 should return 1', () => {
        var result = mathOperations.diff(3, 2)
        expect(result).toBe(1);
    });

    test('30Arjun product 3 * 2 should return 6', () => {
        var result = mathOperations.product(3, 2)
        expect(result).toBe(6);
    });
    
    
    test('31Arjun test added by testtas9812 product 3 * 2 should return 6', () => {
        var result = mathOperations.product(3, 2)
        expect(result).toBe(6);
    });
  
      test('32Arjun adding 1 + 2 should return 3', () => {
        var result = mathOperations.sum(1, 2)
        expect(result).toBe(3);
    });
    
    test('33Arjun difference 3 - 2 should return 1', () => {
        var result = mathOperations.diff(3, 2)
        expect(result).toBe(1);
    });

    test('34Arjun product 3 * 2 should return 6', () => {
        var result = mathOperations.product(3, 2)
        expect(result).toBe(6);
    });
    
    
    test('35Arjun test added by testtas9812 product 3 * 2 should return 6', () => {
        var result = mathOperations.product(3, 2)
        expect(result).toBe(6);
    });
  
      test('36Arjun adding 1 + 2 should return 3', () => {
        var result = mathOperations.sum(1, 2)
        expect(result).toBe(3);
    });
    
    test('37Arjun difference 3 - 2 should return 1', () => {
        var result = mathOperations.diff(3, 2)
        expect(result).toBe(1);
    });

    test('38Arjun product 3 * 2 should return 6', () => {
        var result = mathOperations.product(3, 2)
        expect(result).toBe(6);
    });
    
    
    test('39Arjun test added by testtas9812 product 3 * 2 should return 6', () => {
        var result = mathOperations.product(3, 2)
        expect(result).toBe(6);
    });
  
      test('40Arjun adding 1 + 2 should return 3', () => {
        var result = mathOperations.sum(1, 2)
        expect(result).toBe(3);
    });
    
    test('41Arjun difference 3 - 2 should return 1', () => {
        var result = mathOperations.diff(3, 2)
        expect(result).toBe(1);
    });

    test('42Arjun product 3 * 2 should return 6', () => {
        var result = mathOperations.product(3, 2)
        expect(result).toBe(6);
    });
    
    
    test('43Arjun test added by testtas9812 product 3 * 2 should return 6', () => {
        var result = mathOperations.product(3, 2)
        expect(result).toBe(6);
    });
  
      test('44Arjun adding 1 + 2 should return 3', () => {
        var result = mathOperations.sum(1, 2)
        expect(result).toBe(3);
    });
    
    test('A45rjun difference 3 - 2 should return 1', () => {
        var result = mathOperations.diff(3, 2)
        expect(result).toBe(1);
    });

    test('46Arjun product 3 * 2 should return 6', () => {
        var result = mathOperations.product(3, 2)
        expect(result).toBe(6);
    });
    
    
    test('47Arjun test added by testtas9812 product 3 * 2 should return 6', () => {
        var result = mathOperations.product(3, 2)
        expect(result).toBe(6);
    });
  
      test('48Arjun adding 1 + 2 should return 3', () => {
        var result = mathOperations.sum(1, 2)
        expect(result).toBe(3);
    });
    
    test('49Arjun difference 3 - 2 should return 1', () => {
        var result = mathOperations.diff(3, 2)
        expect(result).toBe(1);
    });

    test('50Arjun product 3 * 2 should return 6', () => {
        var result = mathOperations.product(3, 2)
        expect(result).toBe(6);
    });
    
    
    test('51Arjun test added by testtas9812 product 3 * 2 should return 6', () => {
        var result = mathOperations.product(3, 2)
        expect(result).toBe(6);
    });
  
      test('52Arjun adding 1 + 2 should return 3', () => {
        var result = mathOperations.sum(1, 2)
        expect(result).toBe(3);
    });
    
    test('53Arjun difference 3 - 2 should return 1', () => {
        var result = mathOperations.diff(3, 2)
        expect(result).toBe(1);
    });

    test('54Arjun product 3 * 2 should return 6', () => {
        var result = mathOperations.product(3, 2)
        expect(result).toBe(6);
    });
    
    
    test('55Arjun test added by testtas9812 product 3 * 2 should return 6', () => {
        var result = mathOperations.product(3, 2)
        expect(result).toBe(6);
    });
  
    test('56Arjun adding 1 + 2 should return 3', () => {
        var result = mathOperations.sum(1, 2)
        expect(result).toBe(3);
    });
    
    test('57Arjun difference 3 - 2 should return 1', () => {
        var result = mathOperations.diff(3, 2)
        expect(result).toBe(1);
    });

    test('58Arjun product 3 * 2 should return 6', () => {
        var result = mathOperations.product(3, 2)
        expect(result).toBe(6);
    });
    
    
    test('59Arjun test added by testtas9812 product 3 * 2 should return 6', () => {
        var result = mathOperations.product(3, 2)
        expect(result).toBe(6);
    });
  
      test('60Arjun adding 1 + 2 should return 3', () => {
        var result = mathOperations.sum(1, 2)
        expect(result).toBe(3);
    });
    
    test('61Arjun difference 3 - 2 should return 1', () => {
        var result = mathOperations.diff(3, 2)
        expect(result).toBe(1);
    });

    test('62Arjun product 3 * 2 should return 6', () => {
        var result = mathOperations.product(3, 2)
        expect(result).toBe(6);
    });
    
    
    test('63Arjun test added by testtas9812 product 3 * 2 should return 6', () => {
        var result = mathOperations.product(3, 2)
        expect(result).toBe(6);
    });
  
      test('64Arjun adding 1 + 2 should return 3', () => {
        var result = mathOperations.sum(1, 2)
        expect(result).toBe(3);
    });
    
    test('65Arjun difference 3 - 2 should return 1', () => {
        var result = mathOperations.diff(3, 2)
        expect(result).toBe(1);
    });

    test('66Arjun product 3 * 2 should return 6', () => {
        var result = mathOperations.product(3, 2)
        expect(result).toBe(6);
    });
})
